# Producción — usadas por main.py / webhook / agent en runtime
fastapi
uvicorn[standard]
httpx
python-dotenv
pyjwt
bcrypt

# LLM clients de producción
openai     # DeepSeek (OpenAI-compatible) — agente principal
groq       # Moderación anti-jailbreak (llama-3.1-8b-instant)

# anthropic y google-genai solo para scripts/test_agent.py --modelos (A/B local).
# No instalar en producción — instala manualmente si necesitas correr A/B testing.
